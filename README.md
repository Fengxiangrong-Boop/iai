<div align="center">

# 🏭 IAI - Industrial AI Intelligence

**AI 驱动的工业物联网实时设备诊断与智能运维平台**

[![Python](https://img.shields.io/badge/Python-3.10+-3776AB?style=for-the-badge&logo=python&logoColor=white)](https://python.org)
[![Flink](https://img.shields.io/badge/Flink-2.2.0-E6526F?style=for-the-badge&logo=apache-flink&logoColor=white)](https://flink.apache.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-009688?style=for-the-badge&logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com)
[![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=for-the-badge&logo=docker&logoColor=white)](https://docker.com)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow?style=for-the-badge)](LICENSE)

*从传感器数据采集、实时流计算、AI 智能诊断到自动工单生成的全链路闭环系统*

[快速开始](#-快速部署) · [架构设计](#-系统架构) · [功能特性](#-核心能力) · [API 文档](#-api-接口)

</div>

---

## 📖 项目简介

IAI（Industrial AI Intelligence）是一套面向工业物联网（IIoT）场景的**端到端实时智能体系统**。系统采用大模型驱动的 ReAct 推理模式，结合 MCP（Model Context Protocol）工具链，实现设备异常的自动检测、智能诊断和运维决策闭环。

### 🎯 解决的问题

| 传统模式 | IAI 方案 |
|---------|---------|
| 人工巡检，发现滞后 | ⚡ Flink 实时检测，秒级响应 |
| 依赖老师傅经验诊断 | 🧠 AI 大模型 + 知识库自动诊断 |
| 手工开工单，流程慢 | 🔧 自动生成工单并分级派发 |
| 数据分散，难以追溯 | 📊 Grafana 实时大屏 + MySQL 全链路记录 |

## ✨ 核心能力

- 🔄 **实时数据采集** — 传感器数据双写 Kafka + InfluxDB，零数据丢失
- ⚡ **Flink 流计算** — 异常实时检测（毫秒级）+ 分钟级指标聚合
- 🧠 **AI 智能诊断** — 大模型 (GLM-4 / Ollama) + MCP 工具链 + ReAct 推理循环
- 🔧 **自动工单闭环** — 告警去重 → 多 Agent 诊断 → 决策 → 工单自动生成入库
- 📊 **Grafana 实时大屏** — 温度/震动趋势、告警记录、工单状态一目了然
- 🖥️ **Web 管理后台** — 内置暗色主题 SPA，告警/工单/统计实时查看
- 🔌 **Nacos 服务注册** — 微服务自动注册/心跳/注销
- 🤖 **双模型支持** — 本地 Ollama（开发免费）↔ 云端 GLM-4（生产精准），一键切换

## 🏗️ 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                     📊 Grafana 可视化大屏                        │
│              🖥️ Web 管理后台 (AgentServer /dashboard)            │
└──────────────┬────────────────────────────┬──────────────────────┘
               │                            │
         ┌─────▼─────┐               ┌──────▼──────┐
         │  InfluxDB  │               │    MySQL    │
         │ (时序数据)  │               │ (告警/工单)  │
         └─────▲─────┘               └──────▲──────┘
               │                            │
  ┌────────────┼────────────────────────────┼───────────────┐
  │            │                            │               │
  │   ┌────────▼─────────┐    ┌─────────────▼────────────┐  │
  │   │  Flink Metrics   │    │      AgentServer         │  │
  │   │  Aggregation Job │    │  ┌─────────────────────┐ │  │
  │   │  (1min 窗口聚合)  │    │  │ Diagnostic Agent    │ │  │
  │   └────────▲─────────┘    │  │ (诊断专家 · ReAct)   │ │  │
  │            │              │  ├─────────────────────┤ │  │
  │            │              │  │ Decision Agent      │ │  │
  │            │              │  │ (决策专家)           │ │  │
  │            │              │  ├─────────────────────┤ │  │
  │   ┌────────┴──────────┐   │  │ MCP Tools           │ │  │
  │   │      Kafka        │───┤  │ · 设备查询 (MySQL)   │ │  │
  │   │   (消息总线)       │   │  │ · 遥测数据 (InfluxDB)│ │  │
  │   └────────▲──────────┘   │  │ · 知识库检索 (MySQL) │ │  │
  │            │              │  └─────────────────────┘ │  │
  │            │              │  GLM-4 / Ollama (可切换)  │  │
  │   ┌────────┴──────────┐   └──────────────────────────┘  │
  │   │   DataIngestor    │          ▲                      │
  │   │  (传感器模拟器)    │──────────┘                      │
  │   │  双写 Kafka+Influx │    Flink AnomalyDetectionJob   │
  │   └───────────────────┘    (异常 → HTTP Webhook)        │
  │                                                         │
  └─────────────── Redis (告警去重 5min 冷却) ──────────────┘
```

## 📁 项目结构

```
IAI/
├── docker-compose.yml          # 🐳 全栈一键部署（11 个容器）
├── start_all.sh                # 🚀 应用层启动脚本
├── stop_all.sh                 # 🛑 应用层停止脚本
├── README.md
├── LICENSE
│
├── AgentServer/                # 🧠 AI 智能体服务 (FastAPI)
│   ├── api.py                  #   主入口 & API 路由
│   ├── agents/
│   │   ├── base_agent.py       #   ReAct 循环引擎（含错误去重）
│   │   ├── diagnostic_agent.py #   诊断专家 Agent
│   │   └── decision_agent.py   #   决策专家 Agent
│   ├── services/
│   │   ├── alert_service.py    #   告警管理（Redis去重 + MySQL落盘）
│   │   └── nacos_registry.py   #   Nacos 服务注册/心跳
│   ├── routes/
│   │   └── dashboard.py        #   Web 管理后台（内嵌 SPA）
│   ├── models/
│   │   └── database.py         #   数据库连接池（MySQL + InfluxDB）
│   ├── mcp_server.py           #   MCP 工具服务器
│   ├── .env.example            #   配置模板
│   ├── .env.ollama             #   本地模型配置（免费）
│   ├── .env.cloud              #   云端模型配置（付费）
│   └── requirements.txt
│
├── DataIngestor/               # 📡 数据采集服务
│   ├── sensor_simulator.py     #   传感器模拟器（含异常注入）
│   ├── ingestor.py             #   双写逻辑（Kafka + InfluxDB）
│   └── config.py
│
├── FlinkEngine/                # ⚡ Flink 流计算引擎 (Java 17)
│   ├── pom.xml                 #   Maven 构建（Flink 2.2.0）
│   └── src/main/java/com/iai/flink/
│       ├── AnomalyDetectionJob.java    # 异常检测作业
│       ├── MetricsAggregationJob.java  # 指标聚合作业
│       └── sinks/InfluxDBSink.java     # InfluxDB 自定义 Sink
│
├── deploy/                     # 📦 部署配置
│   ├── init-sql/init.sql       #   MySQL DDL & 初始数据
│   └── grafana/                #   Grafana 配置脚本
│       ├── setup_grafana.sh    #   数据源 + 大屏一键配置
│       └── update_dashboard.sh #   大屏优化脚本
│
└── docs/
    └── architecture_design.md  # 架构设计文档
```

## 🚀 快速部署

### 前置要求

- Docker & Docker Compose v2+
- Python 3.10+
- 智谱AI API Key（[获取地址](https://open.bigmodel.cn)）或使用本地 Ollama（免费）

### 第 1 步：克隆项目

```bash
git clone https://github.com/Fengxiangrong-Boop/iai.git
cd iai
```

### 第 2 步：一键启动基础设施（11 个容器）

```bash
docker compose up -d
docker compose ps   # 确认所有服务 Up
```

### 第 3 步：选择大模型方案

**方案 A：本地 Ollama（开发测试，完全免费 💰）**
```bash
# 拉取中文模型
docker exec ollama ollama pull qwen2.5:7b

# 使用本地模型配置
cd AgentServer
cp .env.ollama .env
```

**方案 B：智谱 GLM-4（生产环境，按量付费）**
```bash
cd AgentServer
cp .env.cloud .env
# 编辑 .env 填入你的 API Key
```

### 第 4 步：启动应用服务

```bash
cd ..
bash start_all.sh
```

### 第 5 步：配置 Grafana 大屏（首次）

```bash
bash deploy/grafana/setup_grafana.sh
```

### 第 6 步：验证系统

```bash
# 发送测试告警
curl -X POST http://localhost:8000/api/v1/alerts \
     -H "Content-Type: application/json" \
     -d '{
  "device_id": "PUMP_01",
  "status": "ANOMALY",
  "temperature": 92.3,
  "vibration": 3.5,
  "timestamp": "2026-02-28T06:53:00Z"
}'

# 查看 AI 诊断过程
tail -f AgentServer/api_server.log
```

## 🔌 服务端口总览

| 服务 | 端口 | 用途 | 地址 |
|------|------|------|------|
| AgentServer | 8000 | AI 诊断 API + 管理后台 | http://localhost:8000/dashboard |
| Grafana | 3000 | 可视化大屏 | http://localhost:3000 (admin/admin123) |
| Kafdrop | 9000 | Kafka 消息浏览 (Web UI) | http://localhost:9000 |
| Flink Dashboard | 8081 | 流计算管理 | http://localhost:8081 |
| Nacos | 8848 | 服务注册中心 | http://localhost:8848/nacos |
| Kafka (INTERNAL) | 9092 | 消息队列 (Docker 内部) | - |
| Kafka (EXTERNAL) | 9094 | 消息队列 (外部客户端) | - |
| InfluxDB | 8086 | 时序数据库 | - |
| MySQL | 3306 | 关系型数据库 | - |
| Redis | 6379 | 告警去重缓存 | - |
| Ollama | 11434 | 本地大模型 | http://localhost:11434 |

## 📡 API 接口

### 告警接入
```bash
POST /api/v1/alerts
Content-Type: application/json

{
  "device_id": "PUMP_01",
  "status": "ANOMALY",
  "temperature": 92.3,
  "vibration": 3.5,
  "timestamp": "2026-02-28T06:53:00Z"
}
```

### 管理后台 API
```bash
GET /api/v1/dashboard/stats      # 系统统计概览
GET /api/v1/dashboard/alerts     # 告警列表（分页）
GET /api/v1/dashboard/orders     # 工单列表（分页）
GET /dashboard                   # Web 管理界面
GET /docs                        # Swagger API 文档
```

## 🎮 运维操作手册

### 日常启停

```bash
# 基础设施
docker compose up -d          # 启动
docker compose down           # 停止
docker compose logs -f kafka  # 查看日志

# 应用服务
bash start_all.sh             # 启动 AgentServer + Flink 作业
bash stop_all.sh              # 停止应用服务

# 传感器模拟（⚠️ 会触发大模型调用）
cd DataIngestor && python sensor_simulator.py
```

### 模型切换

```bash
cd AgentServer
cp .env.ollama .env    # 切换到本地模型（免费）
cp .env.cloud .env     # 切换到云端模型（付费）
# 切换后重启: bash ../stop_all.sh && bash ../start_all.sh
```

### Flink 作业管理

```bash
# 编译（使用 Docker Maven，无需安装 Java）
docker run --rm \
  -v $(pwd)/FlinkEngine:/app \
  -v ~/.m2:/root/.m2 \
  -w /app \
  maven:3.9-eclipse-temurin-17 \
  mvn clean package -DskipTests -s settings.xml

# 查看作业状态
curl -s http://localhost:8081/jobs/overview | python3 -m json.tool
```

## 🛠️ 技术栈

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| AI 引擎 | 智谱 GLM-4 / Ollama | Latest | 智能诊断推理 |
| Agent 框架 | MCP + ReAct | 1.2+ | 工具调用与推理循环 |
| API | FastAPI + Uvicorn | 0.111+ | 异步 HTTP 服务 |
| 流计算 | Apache Flink | 2.2.0 | 实时异常检测 & 指标聚合 |
| 消息队列 | Apache Kafka | Latest | 数据管道 |
| 时序数据库 | InfluxDB | 1.8 | 传感器时序数据 |
| 关系型数据库 | MySQL | 8.0 | 设备/告警/工单 |
| 缓存 | Redis | 7.x | 告警去重（5 分钟冷却） |
| 服务注册 | Nacos | 2.3.2 | 微服务发现 |
| 可视化 | Grafana | 12.4+ | 实时监控大屏 |
| Kafka 可视化 | Kafdrop | Latest | Kafka 消息浏览器 |
| 容器化 | Docker Compose | v2 | 一键部署 |
| 构建 | Maven + Docker | - | Flink JAR 编译 |

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'feat: add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 发起 Pull Request

## 📄 开源许可

本项目基于 [MIT License](LICENSE) 开源，详见 LICENSE 文件。

## 🙏 致谢

- [智谱AI](https://open.bigmodel.cn) — GLM-4 大语言模型
- [Ollama](https://ollama.ai) — 本地大模型运行时
- [Apache Flink](https://flink.apache.org) — 分布式流处理引擎
- [Model Context Protocol](https://modelcontextprotocol.io) — AI 工具调用协议

---

<div align="center">

**如果这个项目对你有帮助，请给一个 ⭐ Star！**

Made with ❤️ by [Fengxiangrong](https://github.com/Fengxiangrong-Boop)

</div>
